[target]
name = "autonomous-development"
path = "/Users/dannyliu/personal_projects/autonomous-development"
branch = "main"
objective = "Build an external intelligence system that monitors AI/agent ecosystem developments (X feeds, GitHub trending, Hacker News, arxiv, release announcements) and connects them to our framework. The system should: (1) discover and aggregate recent advancements in AI agents, tool use, MCP servers, Claude Code, and autonomous coding frameworks, (2) evaluate which developments are relevant to mission-control's architecture and capabilities, (3) generate actionable adaptation proposals (new integrations, pattern adoptions, architecture changes) as structured work units. Use WebSearch and Browser-use MCP tools to scan real sources. Store findings in a new src/mission_control/intelligence/ module with models for sources, findings, and proposals. Include a CLI subcommand `mc intel` that runs a scan and outputs a prioritized list of adaptation opportunities."

[target.verification]
command = ".venv/bin/python -m pytest -q && .venv/bin/ruff check src/ tests/"
timeout = 300
setup_command = "uv sync --extra dev"
setup_timeout = 120

[scheduler]
session_timeout = 86400
cooldown = 5
max_sessions_per_run = 50
model = "opus"
monitor_interval = 5
output_summary_max_chars = 4000

[scheduler.git]
strategy = "branch-per-session"
auto_merge = false

[scheduler.budget]
max_per_session_usd = 99999
max_per_run_usd = 99999

[scheduler.parallel]
num_workers = 8
pool_dir = "/Users/dannyliu/personal_projects/.mc-pool-ads"
warm_clones = 4

[rounds]
max_rounds = 20
stall_threshold = 5
cooldown_between_rounds = 5
max_discovery_chars = 4000
max_discoveries_per_round = 10
max_summary_items = 5
timeout_multiplier = 1.5
stall_score_epsilon = 0.01

[planner]
max_depth = 2
max_children_per_node = 5
budget_per_call_usd = 2.0

[continuous]
max_wall_time_seconds = 86400
timeout_multiplier = 1.5
min_ambition_score = 4
max_replan_attempts = 2
verify_objective_completion = true
max_objective_checks = 2

[review]
gate_completion = true
min_review_score = 3.0

[green_branch]
working_branch = "mc/working"
green_branch = "mc/green"
fixup_max_attempts = 5
auto_push = true
push_branch = "main"

[discovery]
enabled = true
tracks = ["feature", "quality", "security"]
max_items_per_track = 3
min_priority_score = 3.0
model = "opus"
budget_per_call_usd = 5.0
research_enabled = true
research_model = "sonnet"
research_parallel_queries = 3

[heartbeat]
interval = 300
idle_threshold = 3

[notifications.telegram]
# bot_token and chat_id read from env vars TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID
on_heartbeat = true
on_merge_fail = true
on_mission_end = true

[deliberation]
max_rounds = 2
critic_model = "sonnet"

[mcp]
config_path = "/Users/dannyliu/personal_projects/autonomous-development/mcp-workers.json"
enabled = true

[backend]
type = "local"
